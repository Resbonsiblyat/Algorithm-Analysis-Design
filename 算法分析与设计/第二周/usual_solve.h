#ifndef usual_solve_h
#define usual_solve_h

typedef int type;

#include <bits/stdc++.h>
using namespace std;

int findSumofSubseq_u(vector<type>&);

int reverseOrder_u(vector<type>&);

vector<type> disorve(vector<type>&);
#endif
